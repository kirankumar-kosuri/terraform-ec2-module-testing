Terraform EC2 Module Usage – Catalogue

This project demonstrates how to use a reusable Terraform EC2 module (terraform-ec2-module-development) to provision an AWS EC2 instance.

📌 Overview

This configuration:

Uses a custom EC2 module located at ../terraform-ec2-module-development

Launches an EC2 instance

Attaches specified security groups

Applies custom tags

Outputs public and private IP addresses
